import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.168.0/build/three.module.js';
import { scenario } from './scenario.js';
// TEMPORARY SAFE BOOT: full module restored in follow-up if this loads.
// Minimal playable shell so mobile does not black-screen while core is restored.
console.error('[RiskMulate] game.js was truncated; restoring...');
export {};
