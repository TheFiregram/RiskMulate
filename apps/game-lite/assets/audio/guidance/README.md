Factory VO clips (ElevenLabs).

Generate with:

```bash
python3 scripts/generate_factory_audio.py --wait-seconds 180
```

Requires COMPOSIO_API_KEY in `.env.local` and an active ElevenLabs connection.
Clips are played by `focus-guidance.js` when VO is enabled (VO ON).
